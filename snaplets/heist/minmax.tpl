<dfLabel for="${rangeName}"><rangeLabel /></label>
<div id="${rangeName}">
  <dfInput ref="${minlabel}" placeholder="Min" type="number" min="0" step="1" pattern="\d+" val="${minval}"/>
  to
  <dfInput ref="${maxlabel}" placeholder="Max" type="number" min=0 step="1" pattern="\d+" val="${maxval}"/>
</div>
