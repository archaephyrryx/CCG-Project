<label for="${rangeName}"><rangeLabel /></label>
<div id="${rangeName}">
  <input name="${minlabel}" placeholder="Min" type="number" min="0" step="1" pattern="\d+" val="${minval}"/>
  to
  <input name="${maxlabel}" placeholder="Max" type="number" min=0 step="1" pattern="\d+" val="${maxval}"/>
</div>
